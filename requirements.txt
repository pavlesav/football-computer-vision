# Core
numpy>=1.24
opencv-python>=4.8
matplotlib>=3.7
tqdm>=4.65
Pillow>=10.0

# Detection & Tracking
ultralytics>=8.0

# Team Classification (ResNet18 embeddings + KMeans)
torch>=2.0
torchvision>=0.15
scikit-learn>=1.3

# Player re-identification (src/reid.py) — osnet_ain_x1_0 appearance embeddings
# for cross-camera-cut identity. tensorboard is an eager import of torchreid's
# training engine (not used at inference); gdown fetches the pretrained weights.
torchreid>=0.2.5
tensorboard>=2.0
gdown>=4.0

# VLM jersey-number reader (src/jersey_vlm.py) — Qwen2-VL-2B via transformers.
# Reads back-of-shirt numbers easyocr misses (6/8 vs 3/8 GT tracks measured).
transformers>=5.0
accelerate>=1.0

# Interactive Visualization
plotly>=5.0
ipywidgets>=8.0

# Notebook
jupyter>=1.0
ipykernel>=6.0
